on:
  schedule:
  - cron:  "*/5 * * * *"
jobs:
  repo-sync:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: repo-sync
      uses: repo-sync/github-sync@v2
      with:
        source_repo: "KiCAD/kicad-symbols"
        source_branch: "master"
        destination_branch: "master"
        github_token: ${{ secrets.BOT_TOKEN }}